+++
title = 'API page canonical field name guide'
linkTitle = 'LC0063'

[params]
  id = 'LC0063'
  severity = 'Info'
  category = 'Design'
  codeAction = false
  ignoreObsolete = true
+++

When exposing table fields on an API page, it is natural to mirror the source field name — `no` for `"No."`, `name` for `Name`. Microsoft's standard Business Central APIs use canonical names for these common fields. An API consumer integrating your endpoint alongside the standard APIs will encounter inconsistent field names, forcing field-by-field mapping where a single convention would have sufficed.

Rename flagged fields to their canonical equivalents:

| Source table field | Canonical API field name |
|---|---|
| `SystemId` | `id` |
| `Name` | `displayName` |
| `SystemModifiedAt` | `lastModifiedDateTime` |
| Fields containing `No.` | Replace `no`/`No` with `number`/`Number` |

### Example

{{< highlight al "hl_lines=14" >}}
page 50100 "Customer API"
{
    PageType = API;
    EntityName = 'customer';
    EntitySetName = 'customers';
    SourceTable = Customer;

    layout
    {
        area(Content)
        {
            repeater(General)
            {
                field(no; Rec."No.") // API page canonical field name guide [LC0063]
                {
                }
            }
        }
    }
}
{{< /highlight >}}

Rename the field to its canonical name:

{{< highlight al "hl_lines=14" >}}
page 50100 "Customer API"
{
    PageType = API;
    EntityName = 'customer';
    EntitySetName = 'customers';
    SourceTable = Customer;

    layout
    {
        area(Content)
        {
            repeater(General)
            {
                field(number; Rec."No.")
                {
                }
            }
        }
    }
}
{{< /highlight >}}

### When the diagnostic is reported

- The page has `PageType = API`.
- The field expression references `Rec` (the source table).
- The source field name matches one of the canonical mappings and the current API field name does not use the expected canonical name.

### AZ AL Dev Tools

The `alOutline.apiFieldNamesConversion` setting of the [AZ AL Dev Tools/AL Code Outline](https://marketplace.visualstudio.com/items?itemName=andrzejzwierzchowski.al-code-outline) extension can assist with assigning field names:

```json
"alOutline.apiFieldNamesConversion": [
    {
        "searchRegExp": "^no$",
        "newValue": "number"
    },
    {
        "searchRegExp": "No$",
        "newValue": "Number"
    },
    {
        "searchRegExp": "^systemId$",
        "newValue": "id"
    },
    {
        "searchRegExp": "^systemModifiedAt$",
        "newValue": "lastModifiedDateTime"
    }
]
```

### See also

- [API Page / Query](https://alguidelines.dev/docs/bestpractices/api-page/) on alguidelines.dev
- [API Pages - Mandatory fields](https://github.com/anzwdev/al-code-outline/issues/528) on GitHub
