+++
title = 'Integration events must not be declared in codeunits with Access set to Internal (AC0012)'
linkTitle = 'AC0012'

[params]
  severity = 'Warning'
  category = 'Design'
  codeAction = true
  ignoreObsolete = true
+++

An `[IntegrationEvent]` exists so that other extensions can subscribe to it. When the hosting codeunit has `Access = Internal`, external extensions cannot reference the codeunit — the integration point is dead on arrival. Subscribers in dependent apps fail to compile because the codeunit is inaccessible outside the defining module.

If the event is meant for external consumption, remove `Access = Internal`. If it is meant only for the same module (or those listed in `internalsVisibleTo`), declare it as an `[InternalEvent]` instead — the attribute makes the scope explicit.

### Example

The following integration event is declared in an internal codeunit:

{{< highlight al "hl_lines=6" >}}
codeunit 50100 MyCodeunit
{
    Access = Internal;

    [IntegrationEvent(false, false)]
    local procedure OnBeforeSomething() // Integration events must not be declared in codeunits with Access set to Internal [AC0012]
    begin
    end;
}
{{< /highlight >}}

If the event is intended only for the same module, change it to an `[InternalEvent]`:

{{< highlight al "hl_lines=5" >}}
codeunit 50100 MyCodeunit
{
    Access = Internal;

    [InternalEvent(false, false)]
    local procedure OnBeforeSomething()
    begin
    end;
}
{{< /highlight >}}

If the event should remain available to all extensions, remove `Access = Internal` from the codeunit:

{{< highlight al >}}
codeunit 50100 MyCodeunit
{
    [IntegrationEvent(false, false)]
    local procedure OnBeforeSomething()
    begin
    end;
}
{{< /highlight >}}

### Code fix

Two automated fixes are available:

- **Refactor from IntegrationEvent to InternalEvent** — changes the `[IntegrationEvent]` attribute to `[InternalEvent]`, keeping `Access = Internal` on the codeunit.
- **Remove property Access = Internal** — removes the `Access` property from the codeunit, making the integration event accessible to all extensions.

### See also

- [Event Types](https://learn.microsoft.com/dynamics365/business-central/dev-itpro/developer/devenv-event-types) on Microsoft Learn
- [IntegrationEvent Attribute](https://learn.microsoft.com/dynamics365/business-central/dev-itpro/developer/attributes/devenv-integrationevent-attribute) on Microsoft Learn
- [InternalEvent Attribute](https://learn.microsoft.com/dynamics365/business-central/dev-itpro/developer/attributes/devenv-internalevent-attribute) on Microsoft Learn
