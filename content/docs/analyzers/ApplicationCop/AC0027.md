+++
title = 'Use the Tok suffix for token labels (AC0027)'
linkTitle = 'AC0027'

[params]
  severity = 'Info'
  category = 'Design'
  codeAction = false
  ignoreObsolete = true
+++

When a locked label's value matches its variable name, the label is a fixed token — a technical identifier that must never be translated. Naming it with a `Lbl` or `Txt` suffix signals translatable text, which misleads translators working in .xlf files and developers maintaining the code. A translator who sees `ApiVersionLbl` may assume the string is user-facing and unlock it for localization, causing an API call to send a translated string where a fixed English token was expected.

Rename the variable to use the `Tok` suffix instead.

### Example

The following label is locked and its value matches the variable name (after stripping the `Lbl` suffix), but uses the wrong suffix:

{{< highlight al "hl_lines=4" >}}
codeunit 50100 MyCodeunit
{
    var
        ApiVersionLbl: Label 'ApiVersion', Locked = true; // Use the 'Tok' suffix for token labels [AC0027]
}
{{< /highlight >}}

Replace the `Lbl` suffix with `Tok`:

{{< highlight al "hl_lines=4" >}}
codeunit 50100 MyCodeunit
{
    var
        ApiVersionTok: Label 'ApiVersion', Locked = true;
}
{{< /highlight >}}

### When the diagnostic is reported

- The label has `Locked = true`.
- The label text matches the variable name after stripping any [approved suffix](https://learn.microsoft.com/en-us/dynamics365/business-central/dev-itpro/developer/analyzers/codecop-aa0074#remarks) (`Msg`, `Tok`, `Err`, `Qst`, `Lbl`, `Txt`). The comparison is case-insensitive and ignores non-alphanumeric characters in the label text — `MyLabelTxt: Label 'My Label.', Locked = true` also triggers.
- The variable does not already use the `Tok` suffix.

### See also

- [AC0020](../ac0020/) - Labels suffixed with Tok must be locked
- [AC0021](../ac0021/) - Locked Label must have a suffix Tok
- [CodeCop Warning AA0074](https://learn.microsoft.com/en-us/dynamics365/business-central/dev-itpro/developer/analyzers/codecop-aa0074) - TextConst and Label variable names should have an approved suffix
