+++
title = 'Use return value for better error handling'
linkTitle = 'AC0030'

[params]
  id = 'AC0030'
  severity = 'Info'
  category = 'Design'
  codeAction = false
  ignoreObsolete = true
+++

Single-record read methods like `Get()` and `FindFirst()` return an optional Boolean. When the return value is omitted, the platform's behavior changes:

> If you omit this optional return value and the operation does not execute successfully, a runtime error will occur.

— [Record.Get() Method](https://learn.microsoft.com/dynamics365/business-central/dev-itpro/developer/methods-auto/record/record-get-method) on Microsoft Learn

That runtime error is a generic platform message — it tells the user which record is missing but not why it matters or what to do next. Check the return value to provide a specific error message, exit early, or fall back to alternative logic.

### Example

The following code discards the return value of `Get()`:

{{< highlight al "hl_lines=5" >}}
procedure MyProcedure()
var
    Customer: Record Customer;
begin
    Customer.Get('10000'); // Use return value for better error handling [AC0030]
end;
{{< /highlight >}}

To fix this, check the return value and handle the case when the record is not found. When the record is required, raise a clear error:

{{< highlight al "hl_lines=5-6" >}}
procedure MyProcedure(CustomerNo: Code[20])
var
    Customer: Record Customer;
begin
    if not Customer.Get(CustomerNo) then
        Error('Customer %1 was not found.', CustomerNo);
end;
{{< /highlight >}}

When the calling logic can proceed without the record, exit early:

{{< highlight al "hl_lines=5-6" >}}
procedure MyProcedure()
var
    SalesSetup: Record "Sales & Receivables Setup";
begin
    if not SalesSetup.Get() then
        exit;
    // ...
end;
{{< /highlight >}}

### Error handling

To handle errors consider using the [ErrorInfo](https://learn.microsoft.com/dynamics365/business-central/dev-itpro/developer/methods-auto/errorinfo/errorinfo-data-type) data type. This provides a structured way to capture and relay detailed error information. For more practical guidance and examples, check out this article: [ErrorInfo Data Type & Collectible Errors](https://bccaptain.com.au/2022/01/07/errorinfo-data-type-collectible-errors/) by Tomas Kapitan.

{{< highlight al "hl_lines=6-7" >}}
procedure GetCustomer(CustomerNo: Code[20])
var
    Customer: Record Customer;
    MyErrorHandler: Codeunit "My Error Handler";
begin
    if not Customer.Get(CustomerNo) then
        MyErrorHandler.EmitErrorInfo(CustomerNotFound, CustomerNo);
end;
{{< /highlight >}}

In certain scenarios, implementing an [actionable error](https://learn.microsoft.com/dynamics365/business-central/dev-itpro/developer/devenv-actionable-errors) can further enhance the user experience by guiding users toward resolving the issue.

### When the diagnostic is reported

The rule flags any call to a single-record read method where the return value is discarded — the call appears as a standalone statement rather than inside an `if`, an assignment, or another expression.

Affected methods:
- `Record.Get()`
- `Record.GetBySystemId()`
- `Record.Find()`
- `Record.FindFirst()`
- `Record.FindLast()`

### See also

- [LC0097](../../lintercop/lc0097/) - Avoid mixing exit() and named return variable assignments
- [PC0038](../../platformcop/pc0038/) - Not all code paths return a value
- [Record.Get() Method](https://learn.microsoft.com/dynamics365/business-central/dev-itpro/developer/methods-auto/record/record-get-method) on Microsoft Learn
- [Get, Find, and Next Methods](https://learn.microsoft.com/dynamics365/business-central/dev-itpro/developer/devenv-get-find-and-next-methods) on Microsoft Learn
- [if not then exit](https://alguidelines.dev/docs/bestpractices/if-not-find-then-exit/) on alguidelines.dev
